I have to hand this manuscript back to the press with a page number against every passage I flagged, and the edits we accepted have moved all of them. The flagged passages and their drafted lengths are in `/app/input/passage_ledger.csv`, in manuscript order. `/app/input/edit_register.csv` is the edit register: each edit, the passage it was proposed for, its line change, and whether we accepted or deferred it; the edits from the second read are the ones at the bottom. The press's layout rules are in `/app/input/layout_spec.md`. I need two things back. A page register at `/app/page_register.csv` giving, for every flagged passage, the page it now starts on and how it sits on the page. And five figures in `/app/results.json`: how many passages are recorded under each of the four treatments in the layout rules, and the page the manuscript now ends on. The layout for both is in `/app/input/submission_format.md`.
