I have to hand this manuscript back to the press with a page number against every passage I flagged, and the edits we accepted have moved all of them. The flagged passages and their drafted lengths are in `/app/input/passage_ledger.csv`, in manuscript order. `/app/input/edit_register.xlsx` is the edit register we kept through both reads: its Log sheet has each edit, the passage it was proposed for, its line change, and whether we accepted or deferred it, and nothing has been struck from it. Ines's Tally sheet in the same workbook adds up her first read, the accepted line change on every passage and which passages were left with a deferred edit; it's right as far as it goes, and only Tomas's second read needs folding in. The press's layout rules are in `/app/input/layout_spec.md`. I need two things back. A page register at `/app/page_register.csv` giving, for every flagged passage, the page it now starts on and how it sits on the page. And five figures in `/app/results.json`: how many passages are recorded under each of the four treatments in the layout rules, and the page the manuscript now ends on. The layout for both is in `/app/input/submission_format.md`.
