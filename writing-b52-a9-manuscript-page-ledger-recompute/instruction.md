I have to hand this manuscript back to the press with a page number against every passage I flagged, and the edits we accepted have moved all of them. The passages I flagged and their drafted lengths are in `/app/input/passage_ledger.csv`, in the order I flagged them, with the draft line each starts on; every line in it is a stretch I flagged, and none has been merged or dropped. `/app/input/edit_register.xlsx` lists every edit Ines and Tomas proposed across both reads, and `/app/input/edit_thread.txt` is their mail on it: every call either of them made is in there, in their own words, and nothing has been cut from the thread. Tomas's Draft pages sheet in the register has every flag against the draft's pages; he got the flags right, it's the pages that are stale. The press's layout rules are in `/app/input/layout_spec.md`. I need two things back. A page register at `/app/page_register.csv` giving, for every flagged passage, the page it now starts on and how it sits on the page. And five figures in `/app/results.json`: how many passages are recorded under each of the four treatments in the layout rules, and the page the manuscript now ends on. The layout for both is in `/app/input/submission_format.md`.
